# .env (in careconnect_backend)
# —————————————————————————————————————————————————————————————————

# 1) Point SQLAlchemy at your new Postgres database
DATABASE_URL=postgresql://careuser:carepass@localhost:5432/careconnect

# 2) Tell Flask where your app lives
FLASK_APP=src/main.py

# 3) Run in dev mode so you get auto-reload & better errors
FLASK_ENV=development

# 4) Secrets (override the defaults you had in main.py)
SECRET_KEY=changeme-to-a-long-random-string
JWT_SECRET_KEY=changeme-to-another-random-string
